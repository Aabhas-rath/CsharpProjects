package com.topcoder.quartzenergy.mudlog.search;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry level class for the spring-boot application
 */
@SpringBootApplication
public class MudlogServiceBackendApplication {

    /**
     * the main method
     *
     * @param args application start arguments
     */
    public static void main(String[] args) {
        SpringApplication.run(MudlogServiceBackendApplication.class, args);
    }

}
