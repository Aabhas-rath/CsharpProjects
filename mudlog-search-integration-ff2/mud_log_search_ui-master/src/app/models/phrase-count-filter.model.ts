export class PhraseCountFilterModel {
  small: {
    min: number;
    max: number;
  };
  medium: {
    min: number;
    max: number;
  };
  large: {
    min: number;
    max: number;
  };
  xLarge: {
    min: number;
    max: number;
  };
}
