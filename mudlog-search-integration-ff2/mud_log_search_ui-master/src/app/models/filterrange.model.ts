/**
 * class holding filter range bounds retrieved from db
 */
export class FilterRangeBounds {
    maxDepth: number;
    minVintage: number;
    maxVintage: number;
    maxPhraseCount: number;
    maxPhraseScore: number;
}
